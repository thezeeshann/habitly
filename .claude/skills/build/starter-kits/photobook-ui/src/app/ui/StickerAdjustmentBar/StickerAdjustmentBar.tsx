import DeleteSelectedButton from '../DeleteSelectedButton/DeleteSelectedButton';
import BlockBar from '../BlockBar/BlockBar';

function StickerAdjustmentBar() {
  return (
    <BlockBar items={[]}>
      <DeleteSelectedButton />
    </BlockBar>
  );
}
export default StickerAdjustmentBar;
